# Testing View transition API with React

```
$ npm i react react-dom react-router-dom

$ npm install -D webpack webpack-cli

# Babel
$ npm install -D @babel/core @babel/preset-env @babel/preset-react babel-loader

# Esbuild loader
$ npm install -D esbuild-loader

$ npm install -D webpack-dev-server html-webpack-plugin
$ npm install -D copy-webpack-plugin css-loader css-minimizer-webpack-plugin mini-css-extract-plugin sass sass-loader terser-webpack-plugin

# Optionnel!
$ npm install -D eslint eslint-plugin-react
```

